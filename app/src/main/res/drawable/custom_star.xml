<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Estrela vazia -->
    <item android:id="@android:id/background"
        android:drawable="@drawable/icon_star"
        android:width="40dp"
        android:height="40dp"
        />
</layer-list>
